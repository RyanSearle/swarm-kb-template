One markdown file per agent session (`<agent-id>.md`), written on the agent's
own branch. Append-only, never edited by another agent. Planner logs double as
the machine's planning history; grep them instead of keeping state files.
